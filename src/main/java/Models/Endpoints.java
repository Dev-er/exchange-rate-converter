package Models;

public record Endpoints(String baseCode,
                        String targetCode,
                        double conversionResult,
                        double conversionRate) {
}
